# Development tooling, pinned to the versions this project was verified against.
# Runtime dependencies are declared in pyproject.toml, not here.
coverage==7.15.4
mypy==2.3.1
pre-commit==4.6.2
pytest==9.1.1
ruff==0.16.3
